#!/usr/bin/env node
path = require('path');

exports.command = {
  description: 'Create a freshbooks-cli plugin',
};

if (require.main === module) {
  require(path.join(__dirname, '..', 'index.js'));
}
